{
  "name": "functions",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "devDependencies": {
    "twilio-run": "^2"
  },
  "engines": {
    "node": "10.17.0"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "form-data": "^3.0.0"
  }
}
